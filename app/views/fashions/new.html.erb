<h2 class="py-4 text-center">New Post</h2>
<%= form_with model: @fashion, local:true do |f| %>
  <div class="mt-3 text-center form-group">
    <label for="fashion_image">Image</label>
  </div>
  <div class="pb-3 text-center form-group">
    <%= f.attachment_field :fashion_images_images, multiple: true %>
  </div>
  <div class="mt-3 text-center form-group">
    <label for="fashion_opinion">Opinion</label>
  </div>
  <div class="pb-3 text-center form-group">
    <%= f.text_area :introduction %>
  </div>
  <div class="mt-3 text-center form-group">
    <label for="fashion_opinion">Tag</label>
  </div>
  <div class="pb-3 text-center form-group">
    <%= f.text_field :tag_name %>
  </div>
  <div class="py-3 text-center form-group">
    <%= f.submit 'submit' %>
  </div>
<% end %>
<%if @fashion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fashion.errors.count, "error") %> prohibited this fashion from being saved:</h2>
        <ul>
          <% @fashion.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
    </div>
<% end %>